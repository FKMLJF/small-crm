import { StyleSheet } from "react-native"

export default StyleSheet.create({
    view: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
    },

    viewTop: {
        flex: 1,
        justifyContent: 'top',
        alignItems: 'center',
        padding: '15px'
    }
})